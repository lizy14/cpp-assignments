目标：
0.熟悉 STL 的 vector 和 set 容器，复习文件读取操作。
1.题目要求为：
	从文本文件“data.txt”读取一系列整数（文件格式请自动定义），并采用向量vector进行存储。然后，分别实现:
		采用算法(algorithm)库的sort函数进行排序(不去重)，并输出排序结果。
		采用集合(set)进行排序(去重)，并输出排序结果。

如何编程：
1.重载了输出流的 << 运算符（"print_r.h"），可将 set 和 vector 按易读格式输出。
2."data.txt" 的格式规定为：一行一个整数。

结果：
用少量用例手动进行了黑箱测试，结果均正确。