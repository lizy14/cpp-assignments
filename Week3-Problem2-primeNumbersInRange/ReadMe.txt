目标：
输出给定区间内的所有素数。

原理：
1.采用确定性素数判定算法——试除法。

如何编程和运行：
1.复用了 getInteger() 代码。
2.复用了上学期写的 isPrime() 素数判定函数。该函数的正确性已久经考验。
3.编写了 primeNumbersInRange() 函数，接受区间边界，输出该区间内所有素数，以给定的分隔符分隔。
4.采用结构化程序设计方法。

结果：
1.人工验证：
对特定的输入进行了人工验证，即人脑先算一遍，与输出进行比较。
人工验证了(3,30)一组。
通过。
2.自动验证：
利用了科学计算软件 Mathematica，利用该软件的内置函数，可以给出给定区间内的所有素数。
它是闭源的，所以采用输出到文件进行比较的办法。
采用相似的格式输出，使用 Notepad++ 在细节上将输出文件的格式统一，使用 UltraCompare 对这两个输出的文件进行比较。
Mathematica 源代码：见 test\test_mma.nb。
Mathematica 的输出：见 test\Output_mma.txt。
待验证 C++ 程序的输出：见test\testOutput_cpp.log。
采用了 35 组测试用例，通过率 100%。
3.压力测试：
试除法判定素数所需时间极大地依赖于具体的输入，给出“平均用时”没有意义。没有做压力测试。


结论：
本程序可靠。